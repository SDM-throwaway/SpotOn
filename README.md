# SpotOn
Spot On: Defending Against Indirect Prompt Injection With Dynamic Spotlighting

## Setup

Install the project once after cloning:

```bash
uv pip install -e .
```

## Docker

Run from the `bipia_benchmark/` directory:

```bash
cd bipia_benchmark
docker compose up --build
```
