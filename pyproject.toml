[project]
name = "attack-ai-now"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiohttp>=3.13.5",
    "jupyter>=1.1.1",
    "matplotlib>=3.10.9",
    "numpy>=2.4.6",
    "tqdm>=4.0.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["defenses*", "bipia_benchmark*"]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
